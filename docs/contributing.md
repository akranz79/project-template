# Guia de Contribuição

Obrigado por considerar contribuir com este projeto!  
Este documento descreve as práticas recomendadas, padrões e processos utilizados para garantir um fluxo de trabalho organizado e profissional.

---

## 🧱 Estrutura Geral do Projeto

Antes de contribuir, familiarize-se com:
- Padrões de commits
- Estrutura de pastas
- Fluxo de branches
- Como rodar o projeto localmente
- Como subir containers

---

## 🪵 Padrões de Commit (Conventional Commits)

Utilizamos o padrão **Conventional Commits**:

- `feat:` nova funcionalidade  
- `fix:` correção de bug  
- `docs:` mudanças na documentação  
- `refactor:` refatoração sem mudança de comportamento  
- `test:` inclusão ou ajuste de testes  
- `chore:` tarefas internas  
- `style:` ajustes que não alteram lógica  

Exemplo:


---

## 🌿 Fluxo de Branches (Git Flow Simplificado)

- `main` → código estável  
- `develop` → branch para evolução  
- `feature/*` → novas funcionalidades  
- `bugfix/*` → correções  
- `hotfix/*` → correções urgentes  
- `docs/*` → alterações na documentação  

---

## ▶️ Rodando o Projeto Localmente

Para iniciar o ambiente:

```sh
docker compose up -d --build

---

# ❓ **Podemos criar este arquivo agora?**

Se estiver tudo certo, responda **OK** e vou:

1. Criar o arquivo no local correto  
2. Preparar o commit organizado  
3. Subir para o GitHub (caso já tenha configurado o remote)

Aguardando seu **OK**.
